import { Box } from "@chakra-ui/react"

export const ItemHeroBlock = ({ item, ...rest }) => {
  return (
    <Box>

    </Box>
  )
}

export const ItemBlock = ({ item, ...rest }) => {
  return (
    <Box>

    </Box>
  )
}

export const ItemFullBlock = ({ item, ...rest }) => {
  return (
    <Box>

    </Box>
  )
}