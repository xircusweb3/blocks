import { Box, Select } from "@chakra-ui/react"
import { useStorage } from '@xircus-web3/react'

/**
 * List all images uploaded by wallet address to IPFS
 * @param {*} param0 
 * @returns 
 */
export const ImageManager = ({ onSelect }) => {
  const storage = useStorage()

  return (
    <Box>
            
    </Box>
  )
}