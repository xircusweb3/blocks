import { Box } from "@chakra-ui/react";

/**
 * Manage, upload and mint locales
 * @returns 
 */
export default function LocaleManager() {

  // upload to ipfs
  const upload = () => {

  }

  // mint to contract
  // requires: url
  const mint = (assetAddr) => {
    
  }

  const save = () => {

  }

  return (
    <Box>

    </Box>
  )
}