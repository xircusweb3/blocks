import { Box } from "@chakra-ui/react";
import { RightDrawer } from "../components/Drawer";

export default function ThemeEditor({ theme, children, onSave }) {
  return (
    <Box>
      Theme      
    </Box>
  )
}